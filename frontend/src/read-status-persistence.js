import {createClient} from '@supabase/supabase-js';
import {io} from 'socket.io-client';
const supabase=createClient(import.meta.env.VITE_SUPABASE_URL,import.meta.env.VITE_SUPABASE_ANON_KEY);
const API=import.meta.env.VITE_API_URL||'http://localhost:3001';
let currentConversationId=null,busy=false,lastUsername='',socket=null,open=false,markTimer=null,userScrolled=false;
const markedRead=new Set();
async function token(){try{const{data}=await supabase.auth.getSession();return data.session?.access_token||''}catch{return ''}}
async function api(path,opt={}){const t=await token();return fetch(API+path,{...opt,headers:{Authorization:`Bearer ${t}`,'Content-Type':'application/json',...(opt.headers||{})}})}
function username(){return String(document.querySelector('.chat-head small')?.textContent||'').trim().replace(/^@/,'')}
function messagesEl(){return document.querySelector('.messages')}
function nearBottom(){const e=messagesEl();return !!e&&(e.scrollHeight-e.scrollTop-e.clientHeight<180)}
function isClosed(){return document.body.classList.contains('frost-chat-closed')}
function applyRead(messageIds){const ids=new Set((messageIds||[]).map(String));document.querySelectorAll('.messages .msg.mine[data-message-id]').forEach(el=>{const id=String(el.dataset.messageId||'');if(!id||!ids.has(id))return;let s=el.querySelector('.frost-read-status');if(!s){s=document.createElement('span');s.className='frost-read-status';(el.querySelector('.meta')||el).appendChild(s)}s.textContent='✓✓';s.title='Read';s.classList.add('read')})}
function applyRealtimeRead(p){if(!p?.message_id)return;applyRead([p.message_id])}
async function findConversation(){const name=username();if(!name)return null;try{const r=await api('/api/conversations');const j=await r.json().catch(()=>({}));return(j.conversations||[]).find(x=>x.other?.username===name)||null}catch{return null}}
async function sync(){if(busy||isClosed())return;const name=username();if(!name){resetClosed();return}if(name===lastUsername&&currentConversationId)return;busy=true;lastUsername=name;try{const c=await findConversation();if(!c){resetClosed();return}currentConversationId=c.id;markedRead.clear();userScrolled=false;await syncStatusForCurrent()}finally{busy=false}}
async function syncStatusForCurrent(){if(!currentConversationId||isClosed())return;try{const r=await api(`/api/messages/read-status?conversation_id=${encodeURIComponent(currentConversationId)}`);const data=await r.json().catch(()=>({}));if(r.ok)applyRead(data.message_ids||[])}catch{}}
function markVisibleIncomingRead(){if(!open||isClosed()||!currentConversationId||!userScrolled||!nearBottom())return;const e=messagesEl();if(!e||!socket?.connected)return;const ids=[];e.querySelectorAll('.msg.theirs:not(.mine)[data-message-id]').forEach(el=>{const id=String(el.dataset.messageId||'');if(id&&!markedRead.has(id))ids.push(id)});for(const id of ids){markedRead.add(id);let settled=false;const timer=setTimeout(()=>{if(!settled)markedRead.delete(id)},1500);try{socket.emit('message:read',{message_id:id},result=>{settled=true;clearTimeout(timer);if(result?.ok)applyRead([id]);else markedRead.delete(id)})}catch{clearTimeout(timer);markedRead.delete(id)}}}
function scheduleReadCheck(){clearTimeout(markTimer);markTimer=setTimeout(markVisibleIncomingRead,120)}
function attachScroll(){const e=messagesEl();if(!e||e.__frostReadBound)return;e.__frostReadBound=true;e.addEventListener('scroll',()=>{if(isClosed())return;userScrolled=true;scheduleReadCheck()},{passive:true})}
function resetClosed(){open=false;currentConversationId=null;lastUsername='';userScrolled=false;markedRead.clear();clearTimeout(markTimer)}
function detectState(){if(isClosed()){resetClosed();return}const hasChat=!!document.querySelector('.chat-head');if(!hasChat){resetClosed();return}open=true;attachScroll()}
function connectSocket(){token().then(t=>{if(!t)return;socket=io(API,{auth:{accessToken:t},transports:['websocket','polling'],reconnection:true,reconnectionAttempts:Infinity,reconnectionDelay:500,reconnectionDelayMax:4000});socket.on('message:read',p=>{if(isClosed())return;applyRealtimeRead(p)});socket.on('connect',()=>{if(currentConversationId&&!isClosed())socket.emit('join-conversation',currentConversationId)})})}
function boot(){const obs=new MutationObserver(()=>{detectState();clearTimeout(window.__frostReadPersistenceTimer);window.__frostReadPersistenceTimer=setTimeout(()=>{if(!isClosed()){sync();syncStatusForCurrent();scheduleReadCheck()}},100)});obs.observe(document.body,{childList:true,subtree:true});document.addEventListener('frost:chat-closed',resetClosed);document.addEventListener('click',e=>{if(e.target.closest?.('.conversation')){document.body.classList.remove('frost-chat-closed');open=true;userScrolled=false;markedRead.clear();sync()}},true);setInterval(()=>{detectState();if(!isClosed()){syncStatusForCurrent();scheduleReadCheck()}},3000);document.addEventListener('visibilitychange',()=>{});connectSocket();detectState();if(!isClosed())sync()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();